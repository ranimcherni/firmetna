<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}FIRMETNA - Agriculture Tunisienne{% endblock %}</title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- CSS Personnalisé -->
    <link rel="stylesheet" href="{{ asset('css/firmetna.css') }}">
    
    <!-- Custom CSS -->
    <style>
        :root {
            --primary-green: #1a4d2e;
            --dark-green: #007a3e;
            --light-green: #e8f5e9;
            --text-dark: #111827;
        }
        body { font-family: 'Inter', sans-serif; }
        
        .navbar-custom { 
            background: white !important; 
            padding: 0.6rem 0;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }
        
        .navbar-brand {
            display: flex;
            align-items: center;
            text-decoration: none;
        }
        
        .navbar-brand img {
            height: 70px;
            width: auto;
            margin-right: 12px;
        }
        
        .brand-text {
            color: var(--primary-green);
            font-weight: 800;
            font-size: 1.5rem;
            letter-spacing: 1px;
        }
        
        .nav-link {
            font-weight: 600;
            margin: 0 0.75rem;
            color: var(--text-dark) !important;
            transition: all 0.2s;
        }
        .nav-link:hover { color: var(--primary-green) !important; transform: translateY(-1px); }
        
        .btn-connexion {
            background-color: var(--primary-green);
            color: white !important;
            border-radius: 50px;
            padding: 8px 25px;
            font-weight: 700;
            transition: all 0.3s;
            border: none;
        }
        .btn-connexion:hover {
            background-color: #23613a;
            transform: scale(1.05);
            box-shadow: 0 4px 12px rgba(26, 77, 46, 0.2);
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light navbar-custom sticky-top">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <img src="{{ asset('images/logo_hand.png') }}" alt="FIRMETNA Logo">
                <span class="brand-text">FIRMETNA</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}"><i class="fas fa-home me-1"></i>Accueil</a></li>
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="produitsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-shopping-basket me-1"></i>Produits
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="produitsDropdown">
                                <li><a class="dropdown-item" href="{{ path('app_products') }}"><i class="fas fa-store me-2"></i>Marketplace</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_products_vegetale') }}"><i class="fas fa-leaf me-2 text-success"></i>Produits Végétaux</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_products_animale') }}"><i class="fas fa-drumstick-bite me-2 text-danger"></i>Produits Animaux</a></li>
                                {% if app.user and app.user.roleType == 'Agriculteur' %}
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="{{ path('app_products_sell') }}"><i class="fas fa-plus-circle me-2 text-primary"></i>Vendre un produit</a></li>
                                {% endif %}
                            </ul>
                        </li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_events') }}"><i class="fas fa-calendar-alt me-1"></i>Événements</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_forum') }}"><i class="fas fa-comments me-1"></i>Forum</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_donations') }}"><i class="fas fa-hand-holding-heart me-1"></i>Dons</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_partenariats') }}"><i class="fas fa-handshake me-1"></i>Partenariats</a></li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-user-circle"></i> Profil
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-3 mt-2" aria-labelledby="profileDropdown">
                                <li>
                                    <a class="dropdown-item py-2" href="{{ path('app_profil_me') }}">
                                        <i class="fas fa-user-cog me-2 text-success"></i> Mon Compte
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item py-2 d-flex justify-content-between align-items-center" href="{{ path('cart_index') }}">
                                        <span><i class="fas fa-shopping-cart me-2 text-success"></i> Mon Panier</span>
                                        <span class="badge bg-success rounded-pill ms-2">{{ cartService.itemsCount }}</span>
                                    </a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <a class="dropdown-item py-2 text-danger" href="{{ path('app_logout') }}">
                                        <i class="fas fa-sign-out-alt me-2"></i> Déconnexion
                                    </a>
                                </li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item"><a class="nav-link" href="#modules">Modules</a></li>
                        <li class="nav-item ms-lg-2"><a class="btn btn-connexion" href="{{ path('app_login') }}">CONNEXION</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Messages Flash -->
    <div class="container mt-3">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
        {% endfor %}
    </div>

    <!-- Contenu principal -->
    <main>
        {% block body %}{% endblock %}
    </main>

    {% block footer %}
    <!-- Footer -->
    <footer class="text-white py-5 mt-5" style="background-color: #0a2114;">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-4 text-center text-md-start mb-4 mb-md-0">
                    <h5 class="fw-bold mb-3">FIRMETNA</h5>
                    <p class="small text-white-50 mb-0">Cultivez votre passion, gérez votre succès.<br>De notre terre à votre table.</p>
                </div>
                <div class="col-md-4 text-center mb-4 mb-md-0">
                    <div class="d-flex justify-content-center gap-3">
                        <a href="#" class="btn btn-outline-light btn-sm rounded-circle p-2" style="width: 35px; height: 35px; display: flex; align-items: center; justify-content: center;">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a href="#" class="btn btn-outline-light btn-sm rounded-circle p-2" style="width: 35px; height: 35px; display: flex; align-items: center; justify-content: center;">
                            <i class="fab fa-instagram"></i>
                        </a>
                        <a href="#" class="btn btn-outline-light btn-sm rounded-circle p-2" style="width: 35px; height: 35px; display: flex; align-items: center; justify-content: center;">
                            <i class="fab fa-linkedin-in"></i>
                        </a>
                    </div>
                </div>
                <div class="col-md-4 text-center text-md-end">
                    <p class="small text-white-50 mb-0">&copy; 2026 FIRMETNA. <br>Tous droits réservés.</p>
                </div>
            </div>
        </div>
    </footer>
    {% endblock %}

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    {% block javascripts %}{% endblock %}
</body>
</html>