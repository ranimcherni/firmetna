{% extends 'base.html.twig' %}

{% block title %}Mon Espace - FIRMETNA{% endblock %}

{% block body %}
<!-- Global Page Styling -->
<style>
    body {
        background-color: #f8fafc;
        background-image: linear-gradient(rgba(248, 250, 252, 0.95), rgba(248, 250, 252, 0.95)), url('{{ asset('images/hero-bg.png') }}');
        background-size: cover;
        background-attachment: fixed;
    }
    .hero-lavender {
        background: linear-gradient(rgba(107, 70, 193, 0.6), rgba(107, 70, 193, 0.4)), url('https://images.unsplash.com/photo-1499002238440-d264edd596ec?auto=format&fit=crop&q=80&w=2000') no-repeat center center;
        background-size: cover;
        margin-top: -56px;
        min-height: 400px;
        display: flex;
        align-items: center;
        color: white;
    }
    .hero-strawberries {
        background: url('{{ asset('images/boy-strawberries.jpg.png') }}') no-repeat center center;
        background-size: cover;
        min-height: 400px;
        width: 100%;
        display: block;
    }
    .light-lavender-section {
        background-color: #f3f0ff;
        background-image: linear-gradient(rgba(243, 240, 255, 0.85), rgba(243, 240, 255, 0.85)), url('https://images.unsplash.com/photo-1499002238440-d264edd596ec?auto=format&fit=crop&q=80&w=2000');
        background-size: cover;
        background-position: center;
        padding: 100px 0;
        min-height: 250px;
        display: flex;
        align-items: center;
        text-align: center;
        border-bottom: 1px solid #e9daff;
    }
    .slogan-title {
        font-size: 3.5rem;
        font-weight: 800;
        color: #1a202c;
        margin-bottom: 15px;
    }
    .slogan-subtitle {
        font-size: 2.8rem;
        font-weight: 700;
        color: #1a4d2e;
    }
    .max-width-600 { max-width: 600px; }
</style>

<!-- Top Lavender Hero Section -->
<div class="hero-lavender">
    <div class="container py-5 text-white">
        <div class="row align-items-center">
            <div class="col-lg-8">
                <div class="badge bg-white bg-opacity-20 text-white rounded-pill px-3 py-2 mb-3 shadow-sm border border-white border-opacity-25">
                    <i class="fas fa-moon me-2"></i>Espace Distingué FIRMETNA
                </div>
                <h1 class="display-1 fw-bold mb-3">Bonjour, {{ app.user.prenom }} !</h1>
                <h2 class="display-6 fw-bold mb-4">Et bienvenue au FIRMETNA</h2>
                <p class="fs-4 mb-5 opacity-90 max-width-600">Explorez vos modules préférés en haut dans la barre de navigation et gérez votre activité agricole en toute simplicité.</p>
                
                <div class="d-flex gap-3">
                    <a href="{{ path('app_profil_me') }}" class="btn btn-light btn-lg rounded-pill px-5 fw-bold shadow-lg text-primary border-0">
                        <i class="fas fa-user-edit me-2"></i>Mon Profil
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Middle Light Lavender Slogan Section -->
<div class="light-lavender-section">
    <div class="container">
        <h2 class="slogan-title">Firmetna - Produits fermiers frais et de qualité</h2>
        <h3 class="slogan-subtitle" dir="rtl">منتجات زراعية طازجة وعالية الجودة</h3>
    </div>
</div>

<!-- Bottom Strawberries Hero Section -->
<div class="hero-strawberries"></div>
{% endblock %}

