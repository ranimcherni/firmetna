<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Export des Donations - FIRMETNA</title>
    <style>
        @page { margin: 1cm; }
        body { font-family: 'Helvetica', sans-serif; margin: 0; padding: 20px; color: #333; }
        .header { text-align: center; margin-bottom: 30px; border-bottom: 2px solid #1a4d2e; padding-bottom: 20px; }
        .header h1 { color: #1a4d2e; margin: 0; font-size: 28px; }
        .header p { margin: 5px 0 0; color: #666; }
        .stats { margin-bottom: 30px; display: flex; justify-content: space-around; }
        .stat-box { text-align: center; padding: 15px; border: 1px solid #ddd; border-radius: 8px; min-width: 120px; }
        .stat-number { font-size: 24px; font-weight: bold; color: #1a4d2e; }
        .stat-label { font-size: 12px; color: #666; margin-top: 5px; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #f8f9fa; font-weight: bold; color: #1a4d2e; }
        .badge { padding: 4px 8px; border-radius: 4px; font-size: 11px; font-weight: bold; }
        .badge-success { background-color: #d4edda; color: #155724; }
        .badge-danger { background-color: #f8d7da; color: #721c24; }
        .footer { margin-top: 40px; text-align: center; font-size: 12px; color: #666; border-top: 1px solid #ddd; padding-top: 20px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>RAPPORT DES DONATIONS</h1>
        <p>Généré le {{ "now"|date('d/m/Y H:i') }} - FIRMETNA</p>
    </div>

    <div class="stats">
        <div class="stat-box">
            <div class="stat-number">{{ offres|length }}</div>
            <div class="stat-label">Total Offres</div>
        </div>
        <div class="stat-box">
            <div class="stat-number">{{ offres|filter(o => o.disponible == true)|length }}</div>
            <div class="stat-label">Disponibles</div>
        </div>
        <div class="stat-box">
            <div class="stat-number">{{ offres|filter(o => o.disponible == false)|length }}</div>
            <div class="stat-label">Indisponibles</div>
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Catégorie</th>
                <th>Téléphone</th>
                <th>Quantité</th>
                <th>Description</th>
                <th>Statut</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
            {% for offre in offres %}
                <tr>
                    <td>#{{ offre.id }}</td>
                    <td>{{ offre.categorie }}</td>
                    <td>{{ offre.telephone }}</td>
                    <td>{{ offre.quantite ?: '—' }}</td>
                    <td>{{ offre.description|u.truncate(50, '...') }}</td>
                    <td>
                        {% if offre.disponible %}
                            <span class="badge badge-success">Disponible</span>
                        {% else %}
                            <span class="badge badge-danger">Indisponible</span>
                        {% endif %}
                    </td>
                    <td>{{ offre.createdAt ? offre.createdAt|date('d/m/Y') : '—' }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7" style="text-align: center; padding: 40px; color: #666;">
                        Aucune offre de donation trouvée.
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        <p>Ce rapport est généré automatiquement par la plateforme FIRMETNA.</p>
        <p>Pour toute question, contactez l'administrateur du système.</p>
    </div>
</body>
</html>
