{% extends 'base.html.twig' %}

{% block title %}Modifier mon profil - FIRMETNA{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
            <div class="card border-0 shadow-sm rounded-4 overflow-hidden mb-4">
                <div class="bg-success p-4 text-white">
                    <div class="d-flex align-items-center gap-3">
                        <a href="{{ path('app_profil_me') }}" class="btn btn-link text-white p-0">
                            <i class="fas fa-arrow-left fa-lg"></i>
                        </a>
                        <h2 class="fw-bold mb-0">Modifier mes informations</h2>
                    </div>
                </div>
                
                <div class="card-body p-4">
                    <p class="text-muted mb-4 small"><i class="fas fa-info-circle me-1"></i> Mettez à jour vos informations personnelles pour que les autres membres puissent mieux vous connaître.</p>
                    
                    {{ include('profil/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
                </div>
            </div>

            <div class="text-center">
                <a href="{{ path('app_profil_me') }}" class="btn btn-link text-muted text-decoration-none">
                    <i class="fas fa-times me-1"></i> Annuler et retourner au profil
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
