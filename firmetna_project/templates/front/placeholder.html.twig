{% extends 'base.html.twig' %}

{% block title %}{{ module }} - FIRMETNA{% endblock %}

{% block body %}
<style>
    body {
        background-color: #f3f0ff;
        background-image: linear-gradient(rgba(243, 240, 255, 0.9), rgba(243, 240, 255, 0.9)), url('https://images.unsplash.com/photo-1499002238440-d264edd596ec?auto=format&fit=crop&q=80&w=2000');
        background-size: cover;
        background-attachment: fixed;
    }
    .glass-card {
        background: rgba(255, 255, 255, 0.8) !important;
        backdrop-filter: blur(15px);
        border: 1px solid rgba(255, 255, 255, 0.4) !important;
        border-radius: 30px;
    }
</style>

<div class="container-fluid" style="min-height: 100vh; padding-top: 100px;">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-10 text-center">
                <div class="glass-card shadow-2xl p-5 py-5 mb-5">
                    <div class="mb-4">
                        <div class="d-inline-flex align-items-center justify-content-center rounded-circle bg-primary bg-opacity-10 text-primary mb-4" style="width: 120px; height: 120px;">
                            <i class="{{ icon }} fa-4x"></i>
                        </div>
                        <h1 class="display-2 fw-bold text-dark mb-3">{{ module }}</h1>
                        <p class="lead text-muted mb-4 fs-3">{{ description }}</p>
                        <div class="mx-auto bg-primary rounded-pill mb-5" style="width: 100px; height: 6px;"></div>
                    </div>

                    <div class="py-5">
                        <i class="fas fa-tools fa-5x text-primary opacity-20 mb-4 animate-bounce"></i>
                        <h2 class="fw-bold text-dark mb-3">Module en cours de préparation</h2>
                        <p class="text-muted fs-5 mb-5">Nous finalisons les dernières fonctionnalités de cet espace pour vous offrir la meilleure expérience possible.</p>
                        
                        <a href="{{ path('app_front_dashboard') }}" class="btn btn-primary btn-lg rounded-pill px-5 py-3 fw-bold shadow-lg transition-all hover-grow">
                            <i class="fas fa-arrow-left me-2"></i> Retour au Tableau de bord
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
@keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
}
.animate-bounce { animation: bounce 2s infinite; }
.hover-grow:hover { transform: scale(1.05); }
.transition-all { transition: all 0.3s ease; }
</style>
{% endblock %}
